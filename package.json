{
  "name": "react-vite-foundation",
  "version": "0.4.0",
  "description": "Auth-aware API client, local/prod URL switch, query-key factory, and generated-TS-types CLI for a React + Vite + TanStack Query frontend.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "bin": {
    "gen-types": "./bin/gen-types.js"
  },
  "files": [
    "src",
    "bin",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "json-schema-to-typescript": "^15.0.4"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
